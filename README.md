# Platform Edge Gateway

Configuration repository for Hermes platform edge services. This repo captures
service-level settings, pipeline defaults, and operational guardrails used by
the edge gateway and supporting queues.

## What's here
- `platform/` contains Hermes service configuration and runtime defaults.

## Usage
Apply configuration changes via your normal deployment pipeline. Each update is
intended to be reviewed and promoted through environments.
